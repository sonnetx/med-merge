dataset:
  name: chexpert
  source: local
  num_classes: 5
  task_type: multilabel
  image_size: 224
  class_names:
    - Atelectasis
    - Cardiomegaly
    - Consolidation
    - Edema
    - Pleural Effusion
  label_columns:
    - Atelectasis
    - Cardiomegaly
    - Consolidation
    - Edema
    - Pleural Effusion

  # Sherlock paths (override via CLI --data-dir for other environments).
  # OAK-resident because scratch is periodically wiped; csv_path falls back to
  # data_dir/train_valid_combined.csv when unset.
  data_dir: /oak/stanford/groups/roxanad/bikia/processed_chexpert/combined_train_valid_chexpert_v1.0
